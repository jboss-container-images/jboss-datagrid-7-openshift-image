schema_version: 1
name: jolokia
version: 1.0
description: Legacy jolokia script package

modules:
  install:
    - name: jboss.container.jolokia.bash

execute:
  - script: configure.sh
    user: '185'
